// Online Javascript Editor for free
// Write, Edit and Run your Javascript code using JS Online Compiler

//return statement
function hemant() {
    if (true) {
    
    return ;
    //return expression;
    }else{
        return 0;
    }
    
}
console.log(hemant());
function getRectArea(width, height) {
    if (width > 0 && height > 0) {
      return width * height;
    }
    return 0;
  }
  
  console.log(getRectArea(3, 4));
  // Expected output: 12
  
  console.log(getRectArea(-3, 4)); 
//return only works in function
/*The expression whose value is to be returned. If omitted, undefined is returned.
in a function body, the execution of the function is stopped. 
The return statement has different effects when placed in different functions:*/

